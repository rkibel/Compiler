Fn* function_type = new Fn();
    std::vector<Type*> prms;

    StructType* struct_type = new StructType();
    struct_type->name = "ptr";

    Int* int_type = new Int();

    Ptr* ptr_type = new Ptr();
    ptr_type->ref = new Int();

    prms.push_back(struct_type);
    prms.push_back(int_type);
    prms.push_back(ptr_type);

    function_type->prms = prms;

    function_type->ret = new Any();

    std::cout << function_type->type_name();
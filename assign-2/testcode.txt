let x:(int)->int;
struct t{a:int}
fn f(x:int) -> int {
    return 1;
}
fn g(main:int, y:int, z:(int)->int) -> int {
    main(1);
    return z(1);
}
extern e:(int) -> _;
extern k:() -> _;
fn h() -> int {
    x();
    return g(1, 2, f(1));
}
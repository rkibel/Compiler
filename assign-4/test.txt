extern e:(int, int) -> int;
fn f(fval:int) -> int { 
    let x:int;
    let y:int = 2;
    return 3; 
}
let a:int;
fn main() -> int {
    let x:int = 0;
    let y:int = 0;
    let if:&(int)->int = f;
    let ih:&(int)->int = f;
    x = if(a);
    h(55);
    return 0;
}